# GitHub ネイティブのシークレットスキャン除外設定
# ref: https://docs.github.com/en/code-security/secret-scanning/configuring-secret-scanning-for-your-repositories

paths-ignore:
  # Keycloak realm import はローカル開発専用のダミー値を含む
  - "mcp-auth/keycloak/**"
  # .env.example はサンプル値を含むテンプレートファイル
  - "mcp-auth/.env.example"
